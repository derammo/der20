(function() {
    var der20Mode = 'roll20';
    let console = { 
        log: ((message) => {
            let stamp = new Date().toISOString();
            log(`${stamp} der20: ${message}`)
        }), 
        debug: ((message) => {

        }) 
    };
