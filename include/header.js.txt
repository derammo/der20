(function() {
    let console = { 
        log: ((message) => {
            let stamp = new Date().toISOString();
            log(`${stamp} der20: ${message}`)
        }), 
        debug: ((message) => {

        }) 
    };
