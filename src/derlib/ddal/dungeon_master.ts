import { ConfigurationString } from 'derlib/config';

export class DungeonMaster {
    id: string = null;
    name: ConfigurationString = new ConfigurationString();
    dci: ConfigurationString = new ConfigurationString();
}
